services:
  emqx:
    volumes:
      - emqx-data:/opt/emqx/data
      - emqx-logs:/opt/emqx/log
      - ./emqx.conf:/opt/emqx/etc/emqx.conf
      - ${CERT_PEM_PATH}:/opt/emqx/etc/certs/cert.pem
      - ${KEY_PEM_PATH}:/opt/emqx/etc/certs/key.pem